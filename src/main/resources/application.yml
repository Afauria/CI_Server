#默认使用配置
spring:
  profiles:
    active: dev

#公共配置与profiles选择无关
mybatis:
  typeAliasesPackage: com.zwy.ciserver.entity #对应实体类路径
  mapperLocations: classpath:mapper/*.xml

jenkins:
  url: http://localhost:8082/
  user: user
  pwd: pwd
  folders:
    module: AndroidModuleFolder
    project: AndroidProjectFolder
---

#开发配置
spring:
  profiles: dev

  datasource:
    url: jdbc:mysql://localhost:3306/db_ci?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: a410013112
    driver-class-name: com.mysql.cj.jdbc.Driver
    initialSize: 1  #初始化大小
    minIdle: 1  #空闲连接池的大小
    maxActive: 50 #最大激活数量


#pagehelper
pagehelper:
  helperDialect: mysql
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql
