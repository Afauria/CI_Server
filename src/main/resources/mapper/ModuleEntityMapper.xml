<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zwy.ciserver.dao.ModuleEntityMapper">
    <resultMap id="BaseResultMap" type="com.zwy.ciserver.entity.ModuleEntity">
        <result column="module_id" property="moduleId" jdbcType="INTEGER"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="repo" property="repo" jdbcType="VARCHAR"/>
        <result column="branch" property="branch" jdbcType="VARCHAR"/>
        <result column="cur_version" property="curVersion" jdbcType="VARCHAR"/>
        <result column="build_status" property="buildStatus" jdbcType="INTEGER"/>
        <result column="descr" property="descr" jdbcType="VARCHAR"/>
        <result column="gmt_create" property="gmtCreate" jdbcType="TIMESTAMP"/>
        <result column="gmt_update" property="gmtUpdate" jdbcType="TIMESTAMP"/>
    </resultMap>
    <sql id="TABLE">
        t_module
    </sql>
    <sql id="COLUMN">
        module_id, `name`, repo, branch, cur_version, build_status, descr, gmt_create, gmt_update
    </sql>
    <update id="updateModule">
        update
        <include refid="TABLE"/>
        <set>
            `name` = #{name}, repo = #{repo}, branch = #{branch},
            <if test="descr!=null">descr = #{descr},</if>
        </set>
        where module_id = #{moduleId}
    </update>

    <delete id="deleteModuleById">
        delete from
        <include refid="TABLE"/>
        where module_id = #{moduleId}
    </delete>

    <select id="selectModules" resultMap="BaseResultMap">
        select
        <include refid="COLUMN"/>
        from
        <include refid="TABLE"/>
    </select>

    <select id="selectModuleByName" resultType="com.zwy.ciserver.entity.ModuleEntity">
        select
        <include refid="COLUMN"/>
        from
        <include refid="TABLE"/>
        where name = #{name}
    </select>

    <select id="selectModuleById" resultType="com.zwy.ciserver.entity.ModuleEntity">
        select
        <include refid="COLUMN"/>
        from
        <include refid="TABLE"/>
        where module_id = #{moduleId}
    </select>

    <insert id="insertModule" parameterType="com.zwy.ciserver.entity.ModuleEntity" useGeneratedKeys="true"
            keyProperty="moduleId">
        insert into
        <include refid="TABLE"/>
        (`name`, repo, branch, descr)
        values (#{name}, #{repo}, #{branch}, #{descr})
    </insert>

</mapper>